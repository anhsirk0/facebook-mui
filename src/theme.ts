import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
        primary: {
            main: "#3C5898",
            light: "#9CB2CE",
            dark: "#29487D"
        },
        background: {
            default: "#ebebeb",
        }
    }
});

export default theme;
